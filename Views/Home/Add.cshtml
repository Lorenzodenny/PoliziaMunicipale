
@{
    ViewData["Title"] = "Aggiungi Verbale";
}

@model Verbale



<form class="row g-3" asp-controller="Home" asp-action="Add" method="post">
    <div class="col-md-6">
        <label asp-for="@Model.DataViolazione" class="form-label">Data Violazione</label>
        <input type="date" class="form-control" asp-for="@Model.DataViolazione" required>
    </div>
    <div class="col-md-6">
        <label asp-for="@Model.DataTrascrizioneVerbale" class="form-label">Data Trascrizione</label>
        <input type="date" class="form-control" asp-for="@Model.DataTrascrizioneVerbale" required>
    </div>
    <div class="col-12">
        <label asp-for="@Model.IndirizzoViolazione" class="form-label">Indirizzo Violazione</label>
        <input type="text" class="form-control" asp-for="@Model.IndirizzoViolazione" maxlength="50" min="1" placeholder="1234 Main St" required>
    </div>
    <div class="col-12">
        <label asp-for="@Model.Nominativo_Agente" class="form-label">Nominativo Agente</label>
        <input type="text" class="form-control" asp-for="@Model.Nominativo_Agente" maxlength="50" min="1" required>
    </div>
    <div class="col-md-6">
        <label asp-for="@Model.Importo" class="form-label">Importo</label>
        <input type="number" class="form-control" asp-for="@Model.Importo" required>
    </div>
    <div class="col-md-6">
        <label asp-for="@Model.DecurtamentoPunti" class="form-label">Decurtamento Punti</label>
        <input type="number" class="form-control" asp-for="@Model.DecurtamentoPunti">
    </div>

    <div class="col-md-12">
        <label asp-for="@Model.IDAnagrafica" class="form-label"></label>
        <select id="inputState" class="form-select" asp-for="@Model.IDAnagrafica">
            <option selected>Choose...</option>
            @foreach (var utente in ViewBag.Utenti)
            {
                <option value="@utente.IDAnagrafica">@(utente.Nome) - @(utente.Cognome) - @(utente.Cod_Fisc) </option>
            }

        </select>
    </div>

    <div class="col-md-4">
        <label asp-for="@Model.Descrizione" class="form-label"></label>
        <select id="inputState" class="form-select" asp-for="@Model.IDViolazione">
            <option selected>Choose...</option>
            @foreach (var violazione in ViewBag.Violazioni)
            {
                <option value="@violazione.IDViolazione">@violazione.Descrizione</option>
            }

        </select>
    </div>

    <div class="col-12" >
        <button class="btn btn-primary">Aggiungi</button>
    </div>
</form>